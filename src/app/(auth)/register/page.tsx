import { RegisterForm } from "@/components/auth/RegisterForm";

export const metadata = {
  title: "Register | Local Guide",
  description: "Create your Local Guide account",
};

export default function RegisterPage() {
  return <RegisterForm />;
}